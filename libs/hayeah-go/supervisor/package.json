{
  "name": "supervisor-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm workspace for the supervisor library's TypeScript packages (@hayeah/termui + supervisor-webui).",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "overrides": {
      "vite": "npm:@voidzero-dev/vite-plus-core@latest",
      "vitest": "npm:@voidzero-dev/vite-plus-test@latest"
    }
  }
}
