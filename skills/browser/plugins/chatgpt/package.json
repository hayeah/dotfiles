{
  "name": "@browser-plugins/chatgpt",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && BROWSER_PLUGIN_ENTRY=src/conversation.ts vite build && BROWSER_PLUGIN_ENTRY=src/share.ts vite build && BROWSER_PLUGIN_ENTRY=src/common.ts vite build"
  },
  "devDependencies": {
    "vite": "^6",
    "typescript": "^5.7.0"
  }
}
